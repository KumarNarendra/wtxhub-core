version: '2'
services:
    demoservicemaven-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb demoservicemaven-mongodb-config
    demoservicemaven-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    demoservicemaven-mongodb-config:
        image: mongo:4.0.2
        container_name: demoservicemaven-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
