apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-service-maven:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-service-maven:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demo-service-maven
        chartPath: charts/demo-service-maven
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-service-maven"
          image.tag: "{{.DIGEST_HEX}}"
