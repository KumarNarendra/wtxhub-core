apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-local:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-local:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demo-local
        chartPath: charts/demo-local
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/narendra-kumar-becomp-gmail-com/demo-local"
          image.tag: "{{.DIGEST_HEX}}"
